<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['name']        = __( 'Translate Widgets', 'fw' );
$manifest['description'] = __( 'This extension translate Widgets', 'fw' );

$manifest['version'] = '1.0.0';

$manifest['display'] = 'translation';
$manifest['standalone'] = true;